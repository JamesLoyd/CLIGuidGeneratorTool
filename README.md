# GuidGeneratorTool

Generate GUIDS via simple command line tool.

Its as simple as running the following:

```
cli-guid-generate
```
If you would like to generate a specific number of GUIDs, you can specify the number:

```
cli-guid-generate 10
```